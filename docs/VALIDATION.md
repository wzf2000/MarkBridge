# 发布候选验证记录

2026-09-16，0.7.0-rc.1：

- Prettier / PHP 插件、js-beautify、Black 格式检查通过。
- 自有 PHP 语法、版本一致性、私有部署引用检查通过。
- 浏览器资源构建与公式引擎哈希引用检查通过。
- 从 WordPress 7.1 核心脚本重新生成私有运行环境，实际转换通过；通过 PHP 的 bubblewrap 隔离调用检查通过。
- 最终内核 7 种 Markdown 原文/区块往返、3 种内容策略拒绝、Unicode 表情及代码保护检查通过。
- ZIP 实际生成并完成 CRC 检查，附 SHA-256 文件。

本轮没有重新进行完整浏览器编辑闭环、所有主题兼容或普通主机安装矩阵测试。不同版本的验收不能当作当前候选的全部验收。

上述为初次候选整理时的记录。源码现已推送至 GitHub；这不表示已创建 GitHub Release 或完成生产升级。

## 0.7.0-rc.2 · 2026-09-17 短验收

纳入此前完整轮已验证的回收站修复，统一候选版本。格式、语法、构建、发布扫描通过；最终 ZIP 解压后校验 lifecycle.php 与源码一致。在既有隔离 WordPress 加载该 ZIP 代码，执行一次 Markdown 导入与配对保存→回收站→还原→双格式逐字核对→永久删除，全部通过，测试记录已清理。没有重跑浏览器或完整测试矩阵，也没有部署生产。完整隔离测试站升级等未运行项保持原验证边界。

## 后续完整轮与当前边界

2026-09-16 至 2026-09-17，对包含回收站修复的本地候选源码完成了一轮隔离验收：

- 新运行环境构建、转换往返与拒绝策略通过。
- 实际浏览器导入、预览、保存、重开及区块修改后配对保存通过。
- 双格式修订恢复、并发冲突、危险 HTML 拒绝和运行环境失效保护通过。
- 作者权限、原生区块保存与主插件停用后的保护通过。
- 桌面与手机前台公式、代码高亮、复制及 Unicode 表情通过。
- WordPress 实际评论写入与展示过滤链通过。
- 一篇旧版文档的存储契约与加载检查通过。

发现的候选包缺少回收站修复及同版本内容不一致问题，已通过 rc.2 纳入修复并使用独立版本号解决。随后修正 Git 忽略规则误排除插件入口的问题，核对了 Git 归档与最终包的入口、回收站实现一致性。

以下仍未完成，不应写成通过：

- 完整隔离测试站的旧版升级与回退演练（现有证据仅覆盖单文档兼容和停用保护）。
- 匿名用户通过实际浏览器评论表单提交 Markdown（已测 WordPress 写入链）。
- 通过区块界面新建行内公式的完整点击路径（已测导入、源码恢复与预览）。
- WordPress、PHP 与主题的广泛兼容矩阵。

本次文档与格式整理仅运行格式和源码检查，没有重新运行上述完整功能测试，也未更改已生成的 rc.2 安装包。
